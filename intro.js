const db = require('./models')

// db.user.sequelizeMethod()

const userCRUD = async () => {
    try {
        // CREATE 
        // READ
        // UPDATE
        // DESTROY
    } catch(err) {
        console.warn(err)
    }
}